import * as THREE from 'three';
import {RoundedBoxGeometry} from 'three/addons/geometries/RoundedBoxGeometry.js';
import {RoomEnvironment} from 'three/addons/environments/RoomEnvironment.js';
const body=document.body,host=document.querySelector('#scene'),journey=document.querySelector('.journey'),motion=document.querySelector('#motion'),labels=['01 / UNE VISION D’ENSEMBLE','02 / AU SERVICE DES UTILISATEURS','03 / AU CŒUR DE VOTRE PARC','04 / PROTÉGER VOTRE ACTIVITÉ','05 / UNE EXPERTISE À 360°','06 / RETROUVEZ L’ESPRIT LIBRE'];
const media=matchMedia('(prefers-reduced-motion: reduce)');let isStatic=media.matches,renderer,scene,camera,model,parts=[],frame=0,progress=0,visible=true;
const clamp=(v,a=0,b=1)=>Math.min(b,Math.max(a,v)),smooth=(a,b,x)=>{const t=clamp((x-a)/(b-a));return t*t*(3-2*t)};
function updateMode(){body.classList.toggle('static',isStatic);motion.textContent=isStatic?'Activer le parcours animé':'Vue sans animation';motion.setAttribute('aria-pressed',String(isStatic));if(camera)resize();schedule()}
motion.addEventListener('click',()=>{isStatic=!isStatic;updateMode();document.querySelector('#approche').scrollIntoView({behavior:'instant'})});media.addEventListener('change',e=>{isStatic=e.matches;updateMode()});updateMode();
function mat(color,metalness=.0,roughness=.5,extra={}){return new THREE.MeshStandardMaterial({color,metalness,roughness,...extra})}
function box(parent,w,h,d,x,y,z,material,r=.04){const mesh=new THREE.Mesh(new RoundedBoxGeometry(w,h,d,2,Math.min(r,w/3,h/3,d/3)),material);mesh.position.set(x,y,z);parent.add(mesh);return mesh}
function group(name){const g=new THREE.Group();g.name=name;model.add(g);return g}
function part(g,v,start,end){parts.push({g,base:g.position.clone(),offset:new THREE.Vector3(...v),start,end})}
function textTexture(){const c=document.createElement('canvas');c.width=1200;c.height=750;const ctx=c.getContext('2d');ctx.fillStyle='#060a18';ctx.fillRect(0,0,1200,750);const gradient=ctx.createRadialGradient(860,300,20,700,350,700);gradient.addColorStop(0,'#174eff');gradient.addColorStop(.5,'#102966');gradient.addColorStop(1,'#060a18');ctx.fillStyle=gradient;ctx.fillRect(0,0,1200,750);ctx.strokeStyle='#89aaff45';ctx.lineWidth=2;for(let i=0;i<7;i++){ctx.beginPath();ctx.ellipse(920,290,160+i*63,160+i*63,0,0,Math.PI*2);ctx.stroke()}ctx.fillStyle='#aac4ff';ctx.font='22px Arial';ctx.fillText('HUMAIN & CONNECTÉ',85,150);ctx.fillStyle='#ffffff';ctx.font='bold 100px Arial';ctx.fillText('one',80,300);ctx.font='bold 100px Arial';ctx.fillText('tab',257,300);ctx.font='34px Arial';ctx.fillText('Votre informatique.',85,385);ctx.fillStyle='#b4c7ef';ctx.fillText('L’esprit libre.',85,435);ctx.fillStyle='#ffffff20';ctx.fillRect(85,580,1030,1);ctx.font='20px Arial';ctx.fillText('INFOGÉRANCE   /   CYBERSÉCURITÉ   /   CONSEIL',85,630);const texture=new THREE.CanvasTexture(c);texture.colorSpace=THREE.SRGBColorSpace;return texture}
function build(){scene=new THREE.Scene();renderer=new THREE.WebGLRenderer({antialias:true,alpha:true,powerPreference:'low-power'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.75));renderer.setClearColor(0x05070c,0);renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.25;host.appendChild(renderer.domElement);renderer.domElement.setAttribute('aria-hidden','true');renderer.domElement.addEventListener('webglcontextlost',e=>{e.preventDefault();fallback()});
const pmrem=new THREE.PMREMGenerator(renderer),room=new RoomEnvironment();scene.environment=pmrem.fromScene(room,.04).texture;room.dispose();pmrem.dispose();scene.add(new THREE.HemisphereLight(0xc2d6ff,0x142143,2.4));const key=new THREE.DirectionalLight(0xffffff,4);key.position.set(1,8,6);scene.add(key);const rim=new THREE.DirectionalLight(0x315fff,5);rim.position.set(-5,2,-5);scene.add(rim);camera=new THREE.PerspectiveCamera(34,1,.1,100);model=new THREE.Group();scene.add(model);
const silver=mat('#8c96a4',.85,.29),edge=mat('#d0d6df',.9,.23),black=mat('#070b12',.35,.42),keys=mat('#111722',.2,.38),pcb=mat('#093b35',.5,.42),chip=mat('#18243a',.7,.28),blue=mat('#174eff',.5,.3),batteryMat=mat('#151922',.4,.48),gold=mat('#b6a575',.85,.3);
const shell=group('Coque inférieure');box(shell,5.85,.16,3.8,0,-.14,0,silver,.11);box(shell,5.56,.07,3.51,0,-.035,0,black,.08);for(const x of [-2.55,2.55])for(const z of [-1.52,1.52]){box(shell,.3,.035,.27,x,-.23,z,black,.08)}for(const x of [-2.929,2.929])for(let j=0;j<3;j++)box(shell,.012,.06,.23,x,-.09,-1.15+j*.36,black,.008);part(shell,[0,-1.25,0],.02,.24);
const battery=group('Batterie');for(let i=0;i<4;i++){box(battery,1.22,.15,1.6,-1.95+i*1.3,.035,.85,batteryMat,.045);box(battery,.9,.005,.035,-1.95+i*1.3,.115,1.4,silver,.001);for(let j=0;j<3;j++)box(battery,.37,.006,.018,-2.15+i*1.3,.115,.52+j*.055,silver,.001)}part(battery,[0,-.56,.28],.23,.57);
const board=group('Carte mère');box(board,5.25,.085,1.4,0,.07,-.83,pcb,.08);for(let i=0;i<25;i++){const x=-2.4+(i%10)*.52,z=-1.34+Math.floor(i/10)*.43;box(board,.2,.045,.115,x,.14,z,i%3===0?gold:chip,.007)}for(let i=0;i<12;i++){box(board,3.6-i*.13,.006,.009,.1,.116,-1.3+i*.055,gold,.001)}box(board,1.1,.055,.82,0,.15,-.73,chip,.025);part(board,[0,.37,-.06],.2,.45);
const processor=group('Processeur & mémoire');box(processor,.7,.085,.66,0,.23,-.73,edge,.024);box(processor,.49,.012,.46,0,.28,-.73,blue,.014);for(const x of [-.95,.95]){box(processor,.38,.05,.6,x,.2,-.74,chip,.015);for(let i=0;i<7;i++)for(const dx of [-.21,.21])box(processor,.05,.028,.035,x+dx,.2,-.99+i*.08,gold,.005)}part(processor,[0,1.13,-.05],.3,.58);
const fans=group('Ventilateurs');for(const x of [-1.87,1.87]){const fan=new THREE.Group();fan.position.set(x,.18,-.8);fans.add(fan);const casing=new THREE.Mesh(new THREE.CylinderGeometry(.53,.53,.08,40),black);fan.add(casing);const hub=new THREE.Mesh(new THREE.CylinderGeometry(.145,.145,.095,32),silver);hub.position.y=.02;fan.add(hub);for(let i=0;i<19;i++){const a=i/19*Math.PI*2,b=box(fan,.065,.027,.3,Math.sin(a)*.32,.052,Math.cos(a)*.32,silver,.008);b.rotation.y=a+.42}}part(fans,[0,.85,-.22],.27,.53);
const storage=group('Stockage');box(storage,.48,.045,1.15,2.15,.145,.59,pcb,.014);for(let i=0;i<2;i++)box(storage,.36,.055,.37,2.15,.18,.37+i*.45,chip,.012);for(let i=0;i<6;i++)box(storage,.042,.008,.12,1.97+i*.068,.172,1.14,gold,.002);part(storage,[.18,.45,.48],.4,.64);
const keyboard=group('Clavier & repose-poignets');box(keyboard,5.85,.085,3.8,0,.24,0,silver,.1);box(keyboard,4.74,.018,1.8,0,.292,-.63,black,.075);for(let row=0;row<5;row++){for(let col=0;col<13;col++){const x=-2.12+col*.354,z=-1.34+row*.335;box(keyboard,.292,.037,.265,x,.322,z,keys,.027);if(row>0)box(keyboard,.055,.004,.013,x-.06,.344,z-.055,silver,.001)}}box(keyboard,2.2,.008,1.08,0,.288,1.08,edge,.06);box(keyboard,2.15,.009,1.03,0,.293,1.08,silver,.06);for(const x of [-2.61,2.61])for(let row=0;row<20;row++)box(keyboard,.24,.007,.018,x,.29,-1.4+row*.079,black,.002);part(keyboard,[0,1.87,.23],.05,.3);
const lid=group('Écran');lid.position.set(0,.3,-1.78);lid.rotation.x=-.16;box(lid,5.82,3.59,.12,0,1.79,0,silver,.09);box(lid,5.61,3.37,.02,0,1.81,.071,black,.07);const screen=new THREE.Mesh(new THREE.PlaneGeometry(5.38,3.12),new THREE.MeshBasicMaterial({map:textTexture()}));screen.position.set(0,1.83,.085);lid.add(screen);box(lid,.48,.07,.012,0,3.39,.088,black,.02);const webcam=new THREE.Mesh(new THREE.SphereGeometry(.018,12,8),blue);webcam.position.set(0,3.395,.097);lid.add(webcam);for(const x of [-1.8,1.8]){const hinge=new THREE.Mesh(new THREE.CylinderGeometry(.09,.09,.7,16),edge);hinge.rotation.z=Math.PI/2;hinge.position.set(x,.04,.06);lid.add(hinge)}part(lid,[0,1.9,-.42],.04,.32);
// Fine construction guides preserve the assembly axis in the exploded view.
const guides=new THREE.Group();guides.name='Axes d’assemblage';model.add(guides);const guideMat=new THREE.LineDashedMaterial({color:0x4976d6,dashSize:.05,gapSize:.085,transparent:true,opacity:0});for(const x of [-2.52,2.52])for(const z of [-1.4,1.4]){const geo=new THREE.BufferGeometry().setFromPoints([new THREE.Vector3(x,-1.2,z),new THREE.Vector3(x,2.1,z)]);const l=new THREE.Line(geo,guideMat);l.computeLineDistances();guides.add(l)}model.userData.guideMat=guideMat;
resize();body.classList.add('ready');schedule()}
function resize(){if(!renderer)return;const w=host.clientWidth,h=host.clientHeight;if(!w||!h)return;renderer.setSize(w,h,false);camera.aspect=w/h;camera.updateProjectionMatrix();schedule()}
function render(){frame=0;if(!renderer||!visible)return;const viewport=innerHeight,rect=journey.getBoundingClientRect();const chapters=[...document.querySelectorAll('.chapter')],top=-rect.top;let segment=0;for(let i=0;i<chapters.length-1;i++)if(top>=chapters[i].offsetTop)segment=i;const a=chapters[segment].offsetTop,b=chapters[Math.min(segment+1,5)].offsetTop;progress=isStatic?0:clamp((segment+clamp((top-a)/Math.max(1,b-a)))/5);const fold=1-smooth(.81,1,progress);for(const p of parts){const e=smooth(p.start,p.end,progress)*fold;p.g.position.copy(p.base).addScaledVector(p.offset,e)}const spread=smooth(.03,.65,progress)*fold;model.rotation.set(.055*spread,-.37+.15*spread,0);model.userData.guideMat.opacity=spread*.28;const dist=Math.max(15.8,15.8/camera.aspect)*(1+.22*spread);camera.position.set(6.8,5.4,9.8).normalize().multiplyScalar(dist);camera.lookAt(0,1.05+spread*.5,-.05);renderer.render(scene,camera);const idx=Math.min(5,Math.round(progress*5));document.querySelector('#part-label').textContent=labels[idx];document.querySelectorAll('.rail nav a').forEach((a,i)=>{if(i===idx)a.setAttribute('aria-current','step');else a.removeAttribute('aria-current')});host.setAttribute('aria-label',`Ordinateur 3D illustratif — ${labels[idx].split(' / ')[1].toLowerCase()}.`)}
function schedule(){if(!frame&&renderer)frame=requestAnimationFrame(render)}
function fallback(){body.classList.add('no-webgl');isStatic=true;updateMode();motion.hidden=true;document.querySelector('#part-label').textContent='VOTRE SERVICE INFORMATIQUE';if(renderer)renderer.dispose();renderer=null}
try{build()}catch(error){console.warn('La vue 3D est indisponible. Le contenu reste accessible.',error);fallback()}
addEventListener('scroll',schedule,{passive:true});addEventListener('resize',resize);document.addEventListener('visibilitychange',()=>{visible=!document.hidden;if(visible)schedule()});new ResizeObserver(resize).observe(host);
